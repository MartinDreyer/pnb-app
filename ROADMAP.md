# Roadmap

High-level direction for the paint-by-numbers platform (`pnb-app` + `pnb-database` +
`paintbynumbersgenerator-master/backend`). For the day-to-day checklist, see `TODO.md`.
Keep this file and `TODO.md` current — see the "Keeping this current" note in `CLAUDE.md`.

## Done

### 2026-09-01
- Generator backend now only pulls Danish artists from SMK (`filters=[...],[creator_nationality:Danish]` in `paintbynumbersgenerator-master/backend/src/smkClient.ts`) — previously any public-domain, has-image painting could surface, Danish or not.
- Generated and published new puzzles end-to-end through the control panel (Georg Haas, then Christen Købke), confirming the SMK → generate → publish → rotation-schedule pipeline still works.
- Wired real SMK trivia into the post-completion panel: `fetchPaintingTrivia` in `pnb-app/index.html` now looks up the painting's `smk_object_number` against SMK's public API instead of returning placeholder period/style/medium/fun-fact text.

### 2026-09-08
- Removed the topbar logo image (and its now-unused `logoAlt` i18n key/`.logo` CSS) from `pnb-app/index.html`.
- Fixed top/side safe-area clipping on notch/Dynamic Island devices: `.topbar` and `.zoomControls` now pad with `env(safe-area-inset-*)`.
- Set up Xcode 26.6 (Intel-compatible; Xcode 27+ dropped Intel support) and CocoaPods on the dev Mac, and ran the app on a physical iPhone via a free Apple ID (Personal Team) — TestFlight itself still needs a paid Apple Developer Program enrollment, not yet done.
- Added a home/menu screen to `pnb-app/index.html`: opening the app now shows a "Today's puzzle" card plus a history list of past weeks (queried from `rotation_schedule` via new `fetchPuzzleHistory`/`fetchPuzzleById`), instead of loading straight into today's puzzle. Navigation between menu/puzzle is reload-based (`location.hash` + `location.reload()`), since `main()`'s setup was never designed to run twice per page life.
- Added client-side (localStorage) per-puzzle painting progress persistence (`pnb_progress_v1`), since the new menu makes it possible to navigate away from an in-progress puzzle for the first time.

### 2026-09-10
- Replaced the default placeholder Capacitor artwork (the generic blue "X" mark) with a custom app icon and launch splash — a simple four-swatch "paint chip" mark in the app's actual palette colors, generated from one parametric SVG (`rsvg-convert`) into iOS's `AppIcon.appiconset`/`Splash.imageset` and all 11 Android `drawable-*/splash.png` density/orientation buckets.
- Added `@capacitor/splash-screen` and wired `pnb-app/index.html`'s `boot()` to keep the native splash up (`launchAutoHide: false` in `capacitor.config.json`) until translations + puzzle/menu data finish loading, instead of it disappearing instantly and leaving a blank white flash.

### 2026-09-14
- Redesigned `pnb-app`'s home menu to match a "weekly games hub" pattern: today's puzzle plus a horizontally scrollable strip of the 8 most recent past weeks (`menuHistoryStrip`), and a new month-browsable Archive overlay (`fetchPuzzleArchive`/`renderArchiveMonth`) for going further back, grouped by month client-side. Also added a livelier, colorful blurred-gradient background behind the menu/archive screens (previously flat white).
- Added a `generator_config` singleton table (`pnb-database` migration `20260914073000_generator_config.sql`) so the generator backend's automated pipeline is admin-configurable instead of hardcoded: enabled toggle, cadence in days (7=weekly, 1=daily, or custom), number of colors, max facets, min facet size, and a new "max regions per color" cap. `rotation_schedule.cadence`'s check constraint widened to allow `'custom'`.
- Implemented the "max regions per color" cap for real: `FacetReducer.capFacetsPerColor` (`src/facetReducer.ts`) merges a color's smallest facets into their nearest neighbour (reusing the existing `deleteFacet` merge mechanism) until that color's region count is at/under the configured cap; wired into `generate.ts` and a new `Settings.maxFacetsPerColor` field. Verified end-to-end against the local Supabase stack + real SMK API: a puzzle generated with `maxRegionsPerColor=40` came out with 37 regions max for any one color.
- `daily.ts`/`server.ts`'s automated pipeline (`runDailyPreparation`/`ensureDailyPuzzleCoverage`) now reads `generator_config` instead of hardcoded `DAILY_SETTINGS`/24h windows (`getNextDailyWindow` → `getNextRotationWindow(cadenceDays)`), skips entirely when disabled, and the coverage-check interval is decoupled from cadence (checks hourly regardless of whether cadence is daily/weekly/custom, so a config change takes effect promptly). Added a "Settings" section to the admin control panel (`backend/public/index.html`/`app.js`) plus new `GET`/`PUT /api/config` endpoints, and a "max regions per color" field to the existing manual Generate panel.
- Fixed a real "no active puzzle" gap the user hit: after a multi-day period with the backend server not running, the rotation schedule had lapsed with nothing covering "now" until the next manual/hourly coverage check. Also fixed the admin control panel's confusing schedule view that the user diagnosed this through — it listed every row ever created oldest-first with `toLocaleString()`'s locale-dependent dot-separated dates (easy to misread as two different dates) and no indication of past/active/upcoming. Added a combined `GET /api/overview` endpoint (`publish.ts`'s new `listPaintings`/`listUnscheduledPuzzles`) and reworked the panel: the rotation schedule is now newest-first with an explicit Past/Active now/Upcoming pill per row (the active one highlighted) and unambiguous `"Mon, 14 Sep 2026, 09:57"`-style dates, plus two new tables — "Paintings pulled from SMK" (used vs. available) and "Prepared but not used" (generated/published puzzles never actually scheduled).

### 2026-09-15
- Changed the menu/archive history cards in `pnb-app/index.html` to show a plain short date ("14. sep") instead of a "Week of ..."/"Uge ..." label — `formatWeekDate` now formats day+month only (no year, no trailing period) and the `menuWeekOf` i18n key dropped its "Week of"/"Uge" prefix in both `da.json`/`en.json`.
- Cleared all existing paintings/puzzles/rotation-schedule rows and their storage objects from the local `pnb-database` stack, then ran the generator backend's `runDailyPreparation` 5 times to publish 5 fresh weekly puzzles (Samson and the Philistines → Study of a Seated Girl, 2026-09-15 through 2026-10-20).
- Found (not yet fixed) that `runDailyPreparation`/`/api/generate` can crash on `StorageApiError: The object exceeded the maximum allowed size`: SMK's `image_native` download for some paintings is 100s of MB (one hit 413MB), well over the `puzzles` bucket's 50MiB `file_size_limit`. `ensureDailyPuzzleCoverage`'s hourly retry papers over it in practice (next tick just picks a different random painting), but it's a real gap — see TODO.
- Fixed the painting canvas rendering smaller than the full screen on cold app launch (self-corrected after rotating the iPad to landscape and back — user-reported, screenshot showed a big blank strip beside/below the puzzle). Root cause: `--palette-h: clamp(220px, 22vh, 360px)` used plain `vh`, which suffers the exact iPad Safari/WKWebView bug already documented on `.app` (stale pre-layout viewport height until something forces a relayout) — `.app` was already fixed with a `100dvh` override but `--palette-h` never got the same treatment. Added a matching `22dvh` override.
- Superseded this session's earlier date-label change: history/archive cards now show a stable release-order number ("#1", "#2", ...) instead of any date, per the user's follow-up request that the date not appear at all. `fetchNumberedPuzzleHistory` (`pnb-app/index.html`) computes each puzzle's number as its 1-based position in the full `rotation_schedule` ordered by `active_from` ascending (stable as new weeks get appended), independent of which page/limit is being displayed. Added `menuHistorySubtitle`/updated `archiveSubtitle` copy ("A new painting is added every week") to carry the cadence info that used to live in the per-card date. Also replaced the Archive's month-by-month browsing (month header + prev/next nav) with one flat numbered grid, since grouping by month no longer matches how puzzles are labeled — decided with the user via AskUserQuestion rather than assumed.
- Generated 15 more puzzles (`runDailyPreparation` in a loop, same oversized-image skip workaround as before) to bring the total prepared/scheduled backlog from 5 to a round 20, per the user's request to "prepare 20" up front rather than growing the schedule one week at a time. The existing hourly `ensureDailyPuzzleCoverage` check didn't need any code change — it already only preps one puzzle ahead when coverage runs out, which is exactly "a new one every week" once the initial 20-puzzle backlog is in place.
- The cheat swatch now always leaves exactly one region unpainted (`cheatFn` in `pnb-app/index.html` filters to unpainted regions and `.pop()`s the last one before painting the rest), so cheating can never finish the puzzle outright — the user asked for cheat to leave one region for them to paint themselves. Updated the `cheatAriaLabel`/`cheatBottom` copy in both `da.json`/`en.json` to match ("Almost all"/"Næsten alt" instead of "Fill all"/"Udfyld alt").
- Removed the trivia panel's "guess the title/artist before you peek" mini-game per user request — `renderTriviaPanel` now shows the real title/artist immediately on completion instead of behind a guess-then-reveal form, and the associated `guessMatches`/`triviaGuess*` code, CSS, and i18n keys (`triviaGuessPrompt`, `triviaYourGuess`, etc.) were deleted. The separate, still-intact mechanic is hiding the title/artist *while painting* (topbar/loading text) so the finished picture is still a surprise at the end.
- Fixed the "no active puzzle" error message referencing the admin control panel — real end users never have access to it. Reworded to a generic "check back soon" message in both `da.json`/`en.json`.
- Backdated the 20-puzzle backlog's `rotation_schedule` windows so all 20 are usable as an immediate baseline instead of trickling out one per week: puzzle #20 is now "today's puzzle" (`active_from` = the moment this ran) and #1–#19 each got a weekly window stepping back from there, landing fully in the past — so history/archive show all 19 of them right away instead of appearing empty for 19 weeks. Verified `getNextRotationWindow` sees #20 as `coversNow: true` and will prepare #21 automatically once #20's window lapses, so the ongoing weekly cadence picks up cleanly from #21 onward as the user asked.
- The menu's history strip was a single horizontally-scrolling row of cards regardless of screen size, which wastes most of an iPad's width/height (the user's primary device, per a screenshot showing one thin band above a mostly-empty screen). Added a `@media (min-width: 700px)` rule so `.menuHistoryStrip` switches from `flex`+`overflow-x:auto` to a wrapping `grid` (same `minmax(120px, 1fr))` pattern the Archive grid already uses) on tablet-sized screens and up, turning the one band into several. Also raised `fetchRecentPuzzleHistory`'s slice from 8 to 20 so the extra rows actually have puzzles to show instead of wrapping to mostly empty space — phones keep the original single-row scrolling behavior (the media query doesn't apply, and 20 items just scroll further).
- Found and fixed a real portrait-mode layout bug via a user-supplied screen recording (extracted frames with `imageio`/ffmpeg since the Chrome extension isn't connected on this machine): after navigating from the menu into a puzzle, `.app`'s rendered width stuck at roughly 60% of the real device width in portrait, leaving plain white page background beside it (confirmed by sampling pixel colors — the `.canvas::before` decorative gradient's "96%-from-left" blob was positioned consistent with an ~980px-wide container on a 1640px-wide device). Root cause: `.app` used raw `width: 100vw` with no fallback, unlike its `height` which already had the `--app-vh`/`100dvh` JS+CSS pair added for the exact same class of bug (a stale pre-layout viewport value after a `location.reload()`-based navigation, only corrected once something forces a relayout — e.g. rotating). Gave `width` the same treatment: `wireViewportHeightSync` (renamed `wireViewportSizeSync`) now also tracks `--app-vw` from `window.innerWidth`, with a `100dvw` override layered on top.

### 2026-09-15 (later)
- Added an optional AI-researched blurb per painting: `paintbynumbersgenerator-master/backend/src/enrichment.ts` calls Claude (`claude-opus-5`, per the claude-api skill's model default) with the `web_search_20260209` server tool to write a short (2-4 sentence) fact about the painting or painter, wired into `ensurePainting` (`publish.ts`) so it runs once for every newly-pulled painting (both the manual `/api/generate` panel flow and the automated `runDailyPreparation` path go through `ensurePainting`). Deliberately best-effort: no `ANTHROPIC_API_KEY`, a request failure, or an empty result all resolve to `null` and are swallowed — puzzle publishing must never depend on this succeeding. Stored on the new `paintings.ai_summary` column (`pnb-database` migration `20260915120000_painting_ai_summary.sql`); `pnb-app`'s trivia panel (shown on puzzle completion) renders it in its own box with an "AI-generated, may contain mistakes" disclaimer (`triviaAiDisclaimer` in `da.json`/`en.json`) whenever it's present, and shows nothing when it's null.
- Discovered and fixed a real schema-drift gap while implementing the above: `paintings.used_at` (set by `scheduleRotation`, read by `listUsedSmkObjectNumbers` to avoid reusing the same SMK painting) existed in the running local database but had never been captured in a tracked migration — added retroactively as `20260915115000_paintings_used_at.sql`. Found this the hard way: ran `supabase db reset` to apply the new `ai_summary` migration, which (correctly, but destructively) rebuilds the database strictly from tracked migrations and wiped all 20 prepared puzzles in the process, then failed with `column paintings.used_at does not exist` on the first regeneration attempt. Recovered by adding the missing migration and regenerating + re-backdating all 20 puzzles again (see the two "prepare N puzzles" entries above for the method). Lesson for next time: `supabase migration up` applies only pending migrations without wiping data — reach for that instead of `db reset` on a database that isn't disposable.

## Next steps

- **Android launcher icons** — `android/app/src/main/res/mipmap-*/ic_launcher*.png` are still the default Capacitor placeholder; only the splash images were refreshed this round.
- **Package for TestFlight** — enroll the dev Apple ID in the paid Apple Developer Program ($99/yr); Personal Team accounts can't generate App Store distribution profiles.
- **History pagination** — `fetchPuzzleHistory()` is capped at the last 52 weekly puzzles with no pagination; revisit once real usage exceeds a year of history.
- **Timer** — track how long a user takes to complete a puzzle, start to finish.
- **Guessing / leaderboard** — let a user guess the painting mid-fill (before it's fully revealed); rank guessers by how early/accurately they guess, to support a leaderboard.
- **Real rotation reliability** — even with the hourly coverage check, gaps ("no active puzzle") can still happen whenever the backend Node process itself isn't running (e.g. the dev laptop is off/asleep). The lasting fix is the "Cron-first puzzle prep" item below, not a shorter poll interval.
- **Lighter-weight SMK browsing** — the control panel feels heavy when listing multiple candidate paintings. SMK's search API already returns an `image_thumbnail` field alongside `image_native`, but `smkClient.ts`'s `mapItem` currently prefers `image_native` (full resolution) even for browse/search listings. Fix: use `image_thumbnail` for search/random listing previews, and only fetch `image_native` for the one painting actually chosen for generation.
- **Cron-first puzzle prep** — the control panel's manual generate/publish flow should become the override, not the primary path. Default operation should be a scheduled job (daily or weekly, configurable) that prepares and publishes the next puzzle automatically. Note: `ensureDailyPuzzleCoverage`/`runDailyPreparation` in `backend/src/server.ts` already implement a startup + 24h-interval coverage check — next step is making the cadence configurable (daily vs. weekly) and moving scheduling off "only runs while the Node process stays up" onto a real OS/hosting-level cron for reliability.

### 2026-09-17
- Added a `pnb-app` dev/production toggle for startup failures: `window.PNB_CONFIG.ENV` in `config.js` (defaults to `'development'`, the existing raw-technical-detail behavior — the debug banner plus `errorGeneric`'s exposed error message/URLs). Setting it to `'production'` instead shows a new translated, detail-free `#prodErrorOverlay` popup ("We're sorry, there was an error. {{code}}") and fully suppresses the technical debug banner, so a real deployment never leaks internals (SUPABASE_URL, HTTP status text, stack messages) to end users. Each failure path (`errorNoActivePuzzle`/`PUZZLE_NOT_FOUND`, `errorLoadPuzzleFailed`, `errorLoadSvgFailed`, network failures, `file://` protocol, and an `UNKNOWN`/`BOOT_FAILED` catch-all) now carries a short stable `err.code` used as the popup's `{{code}}`, so a user could still reference *something* specific in a support request without any technical detail being exposed. Added `errorGenericProduction`/`errorPopupCloseLabel` to both `da.json`/`en.json`.
- Added `IPAD_QA_CHECKLIST.md`, a manual production-readiness checklist for `pnb-app` on iPad (build/config, cold launch, orientation/layout regressions, painting mechanics, navigation, i18n, failure cases, accessibility, performance, store readiness), plus `PERFORMANCE.md` + `perf/BENCHMARKS.csv`: `index.html`'s `boot()`/`loadAndShow()` now record `performance.mark()` timings for i18n load, puzzle fetch, SVG load/annotate, and total time-to-interactive, surfaced only behind an opt-in `?perf=1` query param (on-screen overlay + a ready-to-paste CSV row logged to console) so a benchmark run can be appended to the CSV and tracked over time in git history without any always-on overhead.
- Ran a real fresh-install test on iPad against the checklist above (screen recording), which surfaced two things worth documenting rather than debugging as bugs: iOS's "Untrusted Developer" alert blocks the very first launch on any device that's never trusted this Apple ID's dev cert before (expected with free Apple ID/Personal Team signing — resolved via Settings → General → VPN & Device Management → Trust), and the very first launch after install/trust shows ~3s of black screen before the splash (iOS validating the fresh binary's code signature before any of our code runs) vs. under 1s on every relaunch after that. Checklist updated with both.
- Changed the cheat swatch's bottom label from "Næsten alt" to "Udfyld (næsten) alt" (`cheatBottom` in `da.json`) per user request, with a `.swatch.cheat .swatchCount` CSS override (smaller font, tighter line-height/gap) so the longer text still fits the swatch's fixed 84×84px box without clipping.
- Added a puzzle serial-number title to the topbar (`#puzzleTitle`, "Maleri #N"/"Painting #N"): fetched via a new `fetchPuzzleNumber()` (same 1-based-position-in-the-rotation-schedule definition as the existing history/Archive numbering) and set non-blockingly via `setPuzzleTitle()` right after the puzzle fetch in `boot()`, so opening an old puzzle from the menu/Archive makes it obvious which numbered puzzle you're looking at, without the network round-trip adding to boot latency and without spoiling the painting's actual title/artist (still only revealed in the completion trivia panel).
